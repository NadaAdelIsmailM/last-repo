class ActionRemoveComponent: public Action {
private:
	**Component ToBeDeleted;
public:
	void Excute() {
		int n;
		n = pManager->GetNoOfSelectedItems;
		ToBeDeleted = new *Component[n];
		ToBeDeleted = pManager->GetNoOfSelectedItems;
	}

};
**Component applicationmanager::GetSelectedItems{
	//for i in range complist if complist[i] is selected then add pointer of component to the list
}
int applicationmanager::GetSelectedItems{
	int c=0;
	//for i in range complist if complist[i] is selected then c++
return c;
}